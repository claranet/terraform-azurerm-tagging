#!/bin/bash
set -e
set -o pipefail

current_tags=""
if [[ ${behavior} == "merge" ]]; then
  az resource tag -i --subscription "${subscription_id}" --tags ${tags} --ids "${resource_id}"
else
  az resource tag --subscription "${subscription_id}" --tags ${tags} --ids "${resource_id}"
fi
